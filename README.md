# 学习笔记
## day01
完成到01variables内容。
注意：
1. 常量需要初始化变量类型。
2. 变量遮蔽相关，1）相当于在作用域内重新新建了一个同名变量。2）后续待补充
3. 变量默认不可改变除非添加mut关键字，结合上一条记忆。
## day02
完成到03if内容。
注意：
1. if后面判断语句不用括号。
2. 返回值可以通过不加;来直接返回。
3. 要多关注函数返回值，变量的数据类型，避免错误。 